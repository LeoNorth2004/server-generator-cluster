
apiVersion: apps/v1
kind: Deployment
metadata:
  name: operations-service
  namespace: generator-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: operations-service
  template:
    metadata:
      labels:
        app: operations-service
    spec:
      containers:
        - name: operations-service
          image: generator-platform/operations-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
          env:
            - name: OPERATIONS_SERVICE_PORT
              value: "8085"
            - name: DB_HOST
              value: "postgres"
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: "postgres"
            - name: DB_PASSWORD
              value: "123456"
            - name: DB_NAME
              value: "generator_platform"
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT
              value: "6379"

---

apiVersion: v1
kind: Service
metadata:
  name: operations-service
  namespace: generator-platform
spec:
  selector:
    app: operations-service
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085
